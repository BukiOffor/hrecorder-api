import { Did } from '@internet-of-people/morpheus-crypto';
import { Crypto, Sdk, Layer2 } from '../types';
export declare class DidDocument implements Layer2.IDidDocument {
    private data;
    private keys;
    constructor(data: Layer2.IDidDocumentData);
    hasRightAt(auth: Crypto.Authentication, right: Sdk.Right, height: number): boolean;
    isTombstonedAt(height: number): boolean;
    fromData(data: Layer2.IDidDocumentData): void;
    toData(): Layer2.IDidDocumentData;
    get height(): number;
    get did(): Did;
    private ensureHeightIsKnown;
    private isKeyValidAt;
    private isRightValidAt;
    private getKeyIdx;
}
//# sourceMappingURL=did-document.d.ts.map