import { Authentication } from './types';
export declare const didToAuth: (did: string) => Authentication;
export declare const isSameAuthentication: (left: Authentication, right: Authentication) => boolean;
/** NOTE throws if conversion failed */
export declare const authenticationFromData: (data: string) => Authentication;
//# sourceMappingURL=authentication.d.ts.map