"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const multibase_1 = tslib_1.__importDefault(require("multibase"));
const sdk_wasm_1 = require("@internet-of-people/sdk-wasm");
exports.encrypt = sdk_wasm_1.encrypt;
exports.decrypt = sdk_wasm_1.decrypt;
exports.encryptString = (plainText, password) => {
    const plainData = Uint8Array.from(Buffer.from(plainText, 'utf8'));
    const cipherData = sdk_wasm_1.encrypt(plainData, password);
    return multibase_1.default.encode('base64url', Buffer.from(cipherData)).toString('ascii');
};
exports.decryptString = (cipherText, password) => {
    const cipherData = Uint8Array.from(multibase_1.default.decode(cipherText));
    const plainData = sdk_wasm_1.decrypt(cipherData, password);
    return Buffer.from(plainData).toString('utf8');
};
//# sourceMappingURL=encrypt.js.map